// LoadingSpinner.js

import React from 'react';

const LoadingSpinner = () => {
  return (
    <div className="loading-spinner">
      {/* You can use a library like react-spinners or any custom loading spinner */}
      Loading...
    </div>
  );
};

export default LoadingSpinner;
